<section class="contact_section__container">
    <div class="container">
        <div class="contact__details__container">

            <div class="email__container">
                <h2>Email</h2>
                <img src=""/>
                <a href="{{the_field()}}">{{the_field('email')}}</a>
            </div>

            <div class="phone_number__container">
                <h2>Phone Number</h2>
                <img src=""/>
                <a href="{{the_field()}}">{{the_field('phone_number')}}</a>
            </div>
        </div>

        <div class="contact__form__container">
            
        </div>
    </div>
</section>